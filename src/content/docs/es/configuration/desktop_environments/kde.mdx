---
title: KDE Plasma
description: Sección de Ayuda de CachyOS KDE Plasma.
ai_translated: true
---

import ImageComponent from '~/components/image-component.astro';
import { Tabs, TabItem, Steps } from '@astrojs/starlight/components';

## Cómo instalar la sesión X11 faltante

Después de que KDE decidiera dividir su desarrollo en múltiples paquetes, la sesión X11 fue eliminada de la instalación por defecto.

```sh
sudo pacman -S plasma-x11-session kwin-x11
```

Al completarlo, X11 se listará como opción en la pantalla de inicio de sesión. 

## Cómo instalar el xorg-server faltante

A partir de la versión 26.01 del instalador de CachyOS, xorg-server ya no se instala y ni plasma-x11-session ni kwin-x11 tratan xorg-server como una dependencia.  Como resultado, si no se instala manualmente el servidor Xorg y sus componentes asociados, se verá una pantalla en negro durante el inicio de sesión.  

```sh
sudo pacman -S xorg-server
```

## Preguntas Frecuentes

### ¿Cómo puedo obtener el mismo aspecto que el que se muestra en las capturas de pantalla de KDE, llamado "Emerald"?

- Es una combinación del tema Emerald + el tema de iconos Qogir que no están instalados por defecto.
  - ```sh title='Instala ambos con el siguiente comando'
    sudo pacman -S cachyos-emerald-kde-theme-git qogir-icon-theme
    ```
  Aplica ambos en `Apariencia y Estilo - Colores y Temas`

### La pantalla de inicio de sesión de KDE no refleja el aspecto de mi escritorio, ¿cómo puedo corregirlo?

:::tip[Esto funciona tanto con SDDM como con Plasma Login Manager.]
:::

Para reflejar el escritorio en la pantalla de inicio de sesión, sigue la captura de pantalla siguiente:

<ImageComponent imgsrc={import('~/assets/images/kde-login-screen-settings.png')} />
