---
title: T2 MacBook
description: So installierst du CachyOS auf einem T2 MacBook
---

import { Steps } from '@astrojs/starlight/components';

CachyOS bietet von Haus aus Unterstützung für T2 MacBooks, einschließlich der notwendigen Kernel-Patches in allen bereitgestellten Kernels. Es gibt jedoch entscheidende Schritte, die für eine erfolgreiche Einrichtung erforderlich sind.

:::caution[Mögliche Risiken]
Das vollständige Entfernen von macOS birgt Risiken, wie Datenverlust oder dass das Gerät unbrauchbar wird. Es wird **dringend empfohlen**, eine macOS-Partition für Firmware-Updates und zur Wiederherstellung beizubehalten. Details findest du in der [T2 Linux Roadmap](https://wiki.t2linux.org/roadmap/#can-i-completely-remove-macos).

Wenn du dich dennoch entscheidest, macOS zu entfernen, stelle sicher, dass du eine vollständige Sicherung hast und erstelle vorab ein [bootfähiges macOS-Installationsprogramm](https://support.apple.com/de-de/101578).
:::

## Vor der Installation

Die Erstellung eines bootfähigen macOS-Installationsprogramms ist optional, wird aber empfohlen, wenn du Partitionen ändern oder macOS entfernen möchtest. Es kann für Firmware-Updates, zur Fehlerbehebung oder zur Systemwiederherstellung nützlich sein. Eine detaillierte Anleitung findest du im [Apple-Leitfaden](https://support.apple.com/de-de/101578).

Führe die folgenden Schritte unter macOS aus, **bevor** du das CachyOS-Installationsprogramm startest.

### Bootfähigen CachyOS-USB-Stick vorbereiten

Lade die [CachyOS ISO](/de/cachyos_basic/download) herunter und folge den Anweisungen unter [Bootfähigen CachyOS-USB-Stick erstellen](/de/installation/installation_prepare#einen-bootfähigen-cachyos-usb-stick-erstellen), um das Installationsmedium zu erstellen.

### (Optional) WLAN-Firmware extrahieren

:::note
WLAN **funktioniert nicht** von Haus aus während oder nach der Installation, da die erforderliche Firmware proprietär ist und von CachyOS nicht weitergegeben werden darf. Du benötigst eine Internetverbindung (z. B. Ethernet, USB-Tethering) für den Installationsprozess, es sei denn, du befolgst die folgenden Schritte, um die WLAN-Firmware vorab vorzubereiten und sie während der Installation zu aktivieren.
:::

Öffne, während du noch in macOS bist, das Terminal und führe den folgenden Befehl aus. Dieser kopiert die notwendigen WLAN-Firmware-Dateien auf deine EFI-Partition, sodass sie später während der CachyOS-Installation zugänglich sind.
```sh
curl -sL https://wiki.t2linux.org/tools/firmware.sh | bash -s copy_to_efi
```

### Deine Festplatte vorbereiten

Wenn du ein Dual-Boot mit macOS planst (empfohlen), musst du Platz für CachyOS schaffen. Verwende das Festplattendienstprogramm in macOS, um die Größe deiner bestehenden macOS-Partition zu ändern:

<Steps>

1.  Öffne das **Festplattendienstprogramm**.
2.  Wähle in der Seitenleiste des Festplattendienstprogramms das **Volume** aus, dessen Größe du ändern möchtest (normalerweise "Macintosh HD" oder ähnlich benannt).
3.  Klicke auf die Schaltfläche **"Partitionieren"** in der Symbolleiste.
4.  Klicke auf die Schaltfläche **"+" (Plus)** unter dem Kuchendiagramm, das deine Festplattennutzung darstellt.
5.  **Wichtig:** Wähle bei der Aufforderung **"Partition hinzufügen"** und *nicht* "Volume hinzufügen". Du musst eine separate Partition für Linux erstellen.
6.  **Name:** Gib einen aussagekräftigen Namen für die neue Partition ein (z. B. "CachyOS" oder "Linux").
7.  **Format:** Wähle ein beliebiges verfügbares Format (wie APFS oder Mac OS Extended). Das CachyOS-Installationsprogramm wird diese Partition später neu formatieren, daher ist die anfängliche Wahl egal.
8.  **Größe:** Weise CachyOS die gewünschte Menge an Speicherplatz zu. **Beachte, dass das Ändern der Partitionsgröße später schwierig oder unmöglich sein kann**, wähle also eine Größe, die deinen Bedürfnissen entspricht.
10. Klicke auf **"Anwenden"**, um die neue Partition zu erstellen. Das Festplattendienstprogramm wird die Größe deiner macOS-Partition ändern und den neuen leeren Speicherplatz erstellen.

</Steps>


### Secure Boot deaktivieren

<Steps>

1. Starte deinen Mac neu und halte sofort nach dem Einschalten `Befehl (⌘) + R` gedrückt, um in den Wiederherstellungsmodus zu gelangen.
2. Gehe zu Dienstprogramme > Startsicherheitsdienstprogramm.
3. Wähle unter **Sicherer Start** die Option "Keine Sicherheit" und unter **Erlaubte Startmedien** die Option "Starten von externen oder Wechselmedien erlauben".

</Steps>

Weitere Details findest du im [Apple-Leitfaden zum Startsicherheitsdienstprogramm](https://support.apple.com/de-in/102522).

---

Für einen zusätzlichen Überblick über die Vorbereitung eines T2-Macs für Linux, schau dir den [t2linux Preinstall-Leitfaden](https://wiki.t2linux.org/guides/preinstall/) an.

## Installationsprozess

<Steps>

1.  **Vom CachyOS-USB-Stick booten:**
    *   Starte deinen Mac neu und halte sofort nach dem Einschalten die `Optionstaste (⌥)` gedrückt.
    *   Wähle den CachyOS-USB-Stick aus (normalerweise als "EFI Boot" bezeichnet).
2.  **(Optional) WLAN in der Live-Umgebung aktivieren:**
    *   Wenn du während der Installation WLAN benötigst und kein Ethernet/Tethering hast, öffne das Terminal, sobald die CachyOS-Live-Umgebung geladen ist.
    *   Führe diese Befehle aus, um die Firmware von der EFI-Partition zu kopieren (erstellt unter [WLAN-Firmware extrahieren](#optional-wlan-firmware-extrahieren)) und den NetworkManager zu konfigurieren:
        ```sh
        # Die EFI-Partition einhängen (normalerweise nvme0n1p1 bei T2 Macs)
        sudo mkdir -p /tmp/apple-wifi-efi
        sudo mount /dev/nvme0n1p1 /tmp/apple-wifi-efi

        # Firmware von EFI in die Live-Umgebung kopieren
        bash /tmp/apple-wifi-efi/firmware.sh get_from_efi
        sudo umount /tmp/apple-wifi-efi

        # NetworkManager für die Verwendung des iwd-Backends konfigurieren
        cat <<EOF | sudo tee /etc/NetworkManager/conf.d/wifi_backend.conf
        [device]
        wifi.backend=iwd
        EOF
        sudo systemctl restart NetworkManager
        ```
    *   Du solltest dich nun über das Netzwerk-Applet in der Taskleiste mit WLAN-Netzwerken verbinden können.
3.  **Das CachyOS-Installationsprogramm ausführen:**
    *   Starte das CachyOS-Installationsprogramm vom Desktop oder aus dem Anwendungsmenü.
    *   Folge dem Standard-Installationsverfahren und beziehe dich dabei auf den Leitfaden [CachyOS Installation Desktop/Laptop](/de/installation/installation_on_root).
    *   Wähle bei der Partitionierung den zuvor erstellten freien Speicherplatz aus. Lass das Installationsprogramm die Formatierung übernehmen.
        :::caution[Vorsicht bei der Partitionierung]
        Sei **sehr vorsichtig** beim Partitionierungsschritt im Installationsprogramm. Stelle sicher, dass du den richtigen freien Speicherplatz auswählst, den du vorbereitet hast, und **lösche oder formatiere nicht** versehentlich deine bestehende macOS-Partition, besonders wenn du ein Dual-Boot beabsichtigst. Überprüfe deine Auswahl doppelt, bevor du fortfährst.
        :::
    *   Die CachyOS Hardware Detection ([chwd](/de/features/chwd/chwd)) sollte während der Installation automatisch die notwendigen T2-spezifischen Boot-Parameter und Konfigurationen anwenden.

</Steps>

## Schritte nach der Installation

Nachdem die Installation abgeschlossen ist und du in dein neues CachyOS-System neu gestartet hast, musst du möglicherweise ein paar zusätzliche Schritte durchführen, um sicherzustellen, dass alles reibungslos funktioniert.

### WLAN-Firmware dauerhaft installieren

Wenn du den optionalen Schritt [WLAN in der Live-Umgebung aktivieren](#installationsprozess) befolgt hast, um die Firmware in der Live-ISO zu erhalten, kannst du ihn einfach erneut ausführen, um WLAN zu aktivieren. Andernfalls verbinde dich mit dem Internet (z. B. über Ethernet, USB-Tethering) und folge den nachstehenden Schritten:

<Steps>
1. Öffne ein Terminal und lade das Firmware-Paket vom Arch Linux T2-Mirror herunter:
   ```sh
   curl https://mirror.funami.tech/arch-mact2/os/x86_64/apple-bcm-firmware-14.0-1-any.pkg.tar.zst -o apple-bcm-firmware-14.0-1-any.pkg.tar.zst
   ```
2. Installiere das heruntergeladene Paket:
   ```sh
   sudo pacman -U apple-bcm-firmware-14.0-1-any.pkg.tar.zst
   ```
3. Lade die WLAN-Kernelmodule neu:
   ```bash
   sudo modprobe -r brcmfmac_wcc
   sudo modprobe -r brcmfmac
   sudo modprobe brcmfmac
   ```
</Steps>

WLAN sollte nun nach Neustarts zuverlässig funktionieren. Du kannst die heruntergeladene `.pkg.tar.zst`-Datei entfernen.

###  Weitere Konfiguration
Zur Konfiguration anderer Hardwarekomponenten wie Audio, Webcam, Touch Bar usw. beziehe dich auf die verschiedenen Anleitungen im [t2linux Wiki](https://wiki.t2linux.org/roadmap/#configuring-the-installation).
