---
title: Downloads und Validierung
description: Download und Validierung des CachyOS-ISOs.
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

## Offizielle ISO-Quellen

Das CachyOS-ISO kann von den folgenden Quellen bezogen werden:

*   [Webseite](<https://cachyos.org/download>)
*   [SourceForge](<https://sourceforge.net/projects/cachyos-arch/files/>)
*   [CachyOS Mirror](<https://mirror.cachyos.org/ISO/>)

## Überprüfung der ISO-Integrität mit SHA256

:::caution[WARNUNG]
Überprüfe immer die Integrität des ISOs, um unerwünschte Probleme bei der Installation oder beim Erstellen eines bootfähigen Mediums zu vermeiden.
:::

**Aktuelle ISO-Version:** `250828`

**SHA256-Hash:** `83ab50783ed41bff0f497b223a78312edef7d4d718f22baf7a6b01a6eb85d88b`

<Tabs>

<TabItem label='Windows'>

<Steps>

1. Lade die [Datei](https://mirror.cachyos.org/ISO/desktop/250828/cachyos-desktop-linux-250828.iso.sha256) herunter, die den SHA256-Hash enthält **(Öffne sie mit einem Texteditor, z. B. Notepad).**
2. Öffne CMD oder PowerShell als Administrator und [navigiere](<https://www.wikihow.com/Change-Directories-in-Command-Prompt>) zu dem Pfad, in dem die ISO- und SHA256-Dateien gespeichert sind.
3. Führe den folgenden Befehl aus:
   ```powershell
   # Beispiel:
   certUtil -hashfile cachyos-desktop-linux-250828.iso SHA256
   ```
4. Vergleiche die Hash-Ausgabe von certUtil mit der aus der heruntergeladenen Datei in **Schritt 1**. Wenn sie übereinstimmen, kannst du mit der Installation von CachyOS fortfahren.

</Steps>

</TabItem>

<TabItem label='Linux'>

<Steps>

1. Lade die [Datei](https://mirror.cachyos.org/ISO/desktop/250828/cachyos-desktop-linux-250828.iso.sha256) herunter, die den SHA256-Hash enthält.
2. Öffne ein Terminal, navigiere in das Verzeichnis, das die `.sha256`-Datei enthält, und führe die folgenden Befehle aus:
   ```sh
   # Beispiel:
   cd Downloads/
   cat cachyos-desktop-linux-250828.iso.sha256
   # 83ab50783ed41bff0f497b223a78312edef7d4d718f22baf7a6b01a6eb85d88b
   ```
3. Vergleiche die Ausgabe aus **Schritt 2** und führe den folgenden Befehl aus, um den Hash der ISO-Datei zu überprüfen:
   ```sh
   # Beispiel:
   sha256sum cachyos-desktop-linux-250828.iso
   ```
4. Wenn die Hashes aus **Schritt 2** und **Schritt 3** übereinstimmen, kannst du mit der Installation von CachyOS fortfahren.

</Steps>

</TabItem>

<TabItem label='macOS'>

<Steps>

1. Lade die [Datei](https://mirror.cachyos.org/ISO/desktop/250828/cachyos-desktop-linux-250828.iso.sha256) herunter, die den SHA256-Hash enthält.
2. Öffne ein Terminal, navigiere in das Verzeichnis, das die `.sha256`-Datei enthält, und führe die folgenden Befehle aus:
   ```sh
   # Beispiel:
   cd Downloads/
   cat cachyos-desktop-linux-250828.iso.sha256
   # 83ab50783ed41bff0f497b223a78312edef7d4d718f22baf7a6b01a6eb85d88b
   ```
3. Vergleiche die Ausgabe aus **Schritt 2** und führe den folgenden Befehl aus, um den Hash der ISO-Datei zu überprüfen:
   ```sh
   # Beispiel:
   shasum -a 256 cachyos-desktop-linux-250828.iso
   ```
4. Wenn die Hashes aus **Schritt 2** und **Schritt 3** übereinstimmen, kannst du mit der Installation von CachyOS fortfahren.

</Steps>

</TabItem>

</Tabs>

## Authentizität des ISO-Abbilds überprüfen (Linux)

Um die Authentizität der ISO-Datei zu überprüfen und sicherzustellen, dass es sich um die offizielle Version des CachyOS-Entwicklungsteams handelt:

<Steps>

1. Importiere den GPG-Schlüssel zur Überprüfung der Authentizität:

   ```shell
   gpg --keyserver hkps://keys.openpgp.org --recv-key F3B607488DB35A47
   ```

2. Lade die ISO-Datei und ihre `.sig`-Signaturdatei herunter und führe den folgenden Befehl aus (ersetze `full_iso_name.iso` durch den tatsächlichen ISO-Dateinamen):
   ```shell
   gpg --verify full_iso_name.iso.sig full_iso_name.iso
   ```

   Wenn du eine **Good signature**-Ausgabe erhältst, ist die ISO-Datei echt:
   ```
   gpg: Signature made jue 28 ago 2025 10:41:14 -03
   gpg:                using RSA key 882DCFE48E2051D48E2562ABF3B607488DB35A47
   gpg: Good signature from "CachyOS <admin@cachyos.org>" [unknown]
   gpg: WARNING: This key is not certified with a trusted signature!
   gpg:          There is no indication that the signature belongs to the owner.
   Primary key fingerprint: 882D CFE4 8E20 51D4 8E25  62AB F3B6 0748 8DB3 5A47
   ```

</Steps>

:::danger
Wenn die Ausgabe keinen **Good signature**-String zurückgibt oder die Schlüssel-ID nicht übereinstimmt, verwende das ISO-Abbild nicht. Stelle sicher, dass das heruntergeladene Abbild
von einer legitimen CachyOS-Quelle stammt, da eine schlechte Signatur darauf hindeuten könnte, dass das ISO manipuliert wurde.
:::
