---
title: KDE Plasma
description: CachyOS KDE Plasma Help Section.
---

import ImageComponent from '~/components/image-component.astro';
import { Tabs, TabItem, Steps } from '@astrojs/starlight/components';

## How to install the missing X11 session

After KDE decided to split their development into multiple packages, the X11 session was removed from the default installation.

```sh
sudo pacman -S plasma-x11-session kwin-x11
```
Upon completion X11 is listed as an option during login screen. 

## How to install the missing xorg-server
As of the 26.01 version of CachyOS installer the xorg-server is no longer installed and neither plasma-x11-session nor kwin-x11 treat xorg-server as a dependency.  As a result failure to install Xorg server & associated components manually will result in a black screen during login.  

```sh
sudo pacman -S xorg-server
```

## FAQ

### How can I get the same look as the one shown in the KDE Screenshots, named as "Emerald"?

- It's a combination of the Emerald Theme + Qogir Icon Theme which are not installed by default.
  - ```sh title='Install both with the following command'
    sudo pacman -S cachyos-emerald-kde-theme-git qogir-icon-theme
    ```
  Apply both in `Appearance & Style - Colors & Themes`

### KDE's Login Screen does not mirror my desktop look, how can I fix that?

:::tip[This works both for SDDM and Plasma Login Manager.]
:::

In order to mirror the desktop on the login screen, follow the screenshot below:

<ImageComponent imgsrc={import('~/assets/images/kde-login-screen-settings.png')} />
