---
title: Průvodce hraním her na CachyOS
description: 'Pokrývá instalaci základních balíčků, hraní her na Steamu s Protonem, různé verze Protonu, Lutris jako centrální místo pro všechny hry a skriptové instalátory pro populární hry.'
---

import ImageComponent from '~/components/image-component.astro';
import MultipleImageComponent from '~/components/multiple-images-component.astro';
import { Tabs, TabItem, Steps } from '@astrojs/starlight/components';

Vítejte v průvodci hraním her na CachyOS. Provedeme vás základními aspekty, jak vše nastavit a užít si skvělý zážitek.

## Předpoklady

:::caution
Než se pustíme do zábavných věcí, je nezbytné zajistit, aby byly ovladače vaší grafické karty správně nainstalovány a funkční. Pokud vaše grafická karta nepracuje optimálně, můžete se při hraní her setkat s problémy.
:::

### Základní balíčky

Abychom vám to usnadnili, CachyOS seskupil všechny potřebné balíčky pro hraní her do jednoho meta balíčku, který obsahuje všechny nezbytné závislosti a knihovny pro hraní na Linuxu, a samostatného balíčku pro nástroje a spouštěče/obchody. Díky tomu je instalace a nastavení pro hraní rychlejší a méně komplikované pro všechny.

*Pokud zjistíte, že nějaké balíčky chybí, neváhejte o tom dát vědět týmu CachyOS.*

Následujte níže uvedené kroky pro zahájení nastavení pro hraní her.
<Tabs>
    <TabItem label='Meta balíček'>
        ```sh
        sudo pacman -S cachyos-gaming-meta
        ```
    </TabItem>
    <TabItem label='Nástroje a obchody'>
        *Tento balíček obsahuje následující:*
            - Gamescope, Goverlay, Heroic Games Launcher, Lutris, MangoHud a Steam.
        ```sh
        sudo pacman -S cachyos-gaming-applications
        ```
    </TabItem>
    <TabItem label= 'CachyOS Hello'>
    - Přejděte do **Apps/Tweaks** a klikněte na `Install Gaming packages`

    *CachyOS Hello nainstaluje oba balíčky `cachyos-gaming-meta` a `cachyos-gaming-applications`*
    </TabItem>
</Tabs>

## Steam FAQ a tipy

:::note
Uživatelé notebooků s diskrétními GPU NVIDIA by se měli řídit následujícím [průvodcem](https://wiki.cachyos.org/configuration/dual_gpu/)
:::

:::tip
CachyOS poskytuje různé verze Protonu, které sestavujeme a udržujeme pro další zlepšení výkonu:
- `proton-cachyos` `(zahrnuto v balíčku cachyos-gaming-meta)` je ta, kterou vyvíjíme s přidanými změnami pro kvalitu hraní (QoL) + pečlivě vybranými patchi + optimalizacemi při kompilaci `(x86-64-v3 & x86-64-v4)`
- `proton-ge-custom`, `proton-tkg-git`, `proton-experimental` a `stabilní vydání protonu` jsou sestaveny oproti nativním knihovnám.
- `proton-ge-custom-bin` je stažen z AUR.
:::

:::tip
Chcete-li zkontrolovat, zda je vaše hra kompatibilní nebo jak běží na Linuxu, navštivte **[ProtonDB](https://www.protondb.com/)** nebo **[Are We Anti-Cheat Yet?](https://areweanticheatyet.com/).**
:::

### Jakou verzi Protonu použít ve Steamu?

- [`proton-cachyos`](https://github.com/CachyOS/proton-cachyos) Jelikož je to verze, kterou sestavujeme a udržujeme, je vysoce doporučená, pokud nemá problém s hrou, kterou chcete hrát. Měla by fungovat ve většině případů.
- [`proton-cachyos-slr`](https://github.com/CachyOS/proton-cachyos/releases) Pro hry používající **BattlEye** nebo **Easy Anti-Cheat**.
- `Proton Experimental` Nejnovější vývojová verze od `Valve`. Pokud je hra, kterou chcete hrát, relativně nová nebo nefunguje dobře s aktuálním stabilním vydáním Protonu, nebo pokud jste viděli, že ji lidé doporučují na [ProtonDB](https://www.protondb.com/).
- `Proton 9.0.4 nebo nižší` Stabilní vydání od `Valve`. Pokud hra, kterou chcete hrát, funguje pouze v předchozím vydání Protonu.
- `Proton-GE` Sestavení od [GloriousEggroll](https://github.com/GloriousEggroll/proton-ge-custom), v některých případech se hodí.

### Jak správně nastavit více možností spuštění
Možnosti spuštění ve Steamu jsou sestaveny podle následujícího vzoru:
- `<proměnné prostředí> <wrappery> %command% <argumenty aplikace>`
    - `<proměnné prostředí>`: Jsou to volby ve formátu `PROMĚNNÁ=hodnota`. Například `PROTON_DXVK_D3D8=1` nebo `DXVK_HUD="fps,memory,version,api"`
    - `<wrappery>`: Aplikace a skripty, které mění způsob spuštění skutečné aplikace. Argumenty pro wrapper obvykle následují za spustitelným souborem wrapperu. Například `mangohud --dlsym` nebo `gamescope -W 1680 -H 1050 -w 1280 -h 720 -S fit -F fsr --mangoapp --`
    - `%command%`: Toto je skutečná aplikace. Měla by být specifikována přesně takto a Steam ji nahradí správným příkazem při spuštění aplikace.
    - `<argumenty aplikace>`: Jsou to různé argumenty pro skutečnou aplikaci a závisí na dané aplikaci. Například `-dx11` by mělo být zde, pokud to aplikace podporuje.

- Příklad: `__GL_SHADER_DISK_CACHE_SKIP_CLEANUP=1 prime-run game-performance %command% -dx11`

:::caution
Nepřidávejte více `%command%` jako oddělovače pro více možností spuštění.

Příklad: `__GL_SHADER_DISK_CACHE_SKIP_CLEANUP=1 %command% game-performance %command% prime-run %command%`
    - V tomto případě bude vše za prvním `%command%` ignorováno, protože je to považováno za argumenty hry, a proto byla jako možnost spuštění rozpoznána pouze `__GL_SHADER_DISK_CACHE_SKIP_CLEANUP=1`.
:::

### Oprava zadrhávání způsobeného funkcí nahrávání her ve Steamu

```sh title='Přidejte do své hry následující možnost spuštění'
LD_PRELOAD="" %command%
```

### Přednačítání shaderů s Proton-CachyOS, -GE a -EM

:::note
Doporučuje se vypnout funkci `Přednačítání shaderů Steamu`, když používáte Proton-CachyOS, Proton-GE nebo Proton-EM, protože již obsahují všechny potřebné kodeky pro přehrávání videí ve hře a v dnešní době by relativně moderní kombinace GPU-CPU měla být více než schopna zvládnout kompilaci shaderů během hry.

Ale pokud máte čas nazbyt, můžete tento tip ignorovat a nechat Steam předkompilovat některé shadery pro vaši hru.

**`Mějte na paměti, že ne všechno se během tohoto procesu uloží do mezipaměti.`**
:::

**Zde je návod, jak tuto funkci vypnout:**

<ImageComponent imgsrc={import('~/assets/images/steam-setting.png')} />
<ImageComponent imgsrc={import('~/assets/images/steam-shader-cache.png')} />

## Tipy pro výkon

### Přepínání napájecích profilů na vyžádání

:::note
Toto chování je mírně odlišné s `intel_pstate`. Na Intelu zůstává governor na `powersave`, ale hodnoty
EPP/EPB jsou nastaveny na `performance`.
:::

CachyOS poskytuje wrapper skript [`game-performance`](https://github.com/CachyOS/CachyOS-Settings/blob/master/usr/bin/game-performance),
který používá `power-profiles-daemon` k dočasnému přepnutí aktuálního napájecího profilu na `performance`.
Profil `performance` zvyšuje úrovně napájení systému a mění CPU governor na `performance`.

Když je tento skript použit ke spuštění hry, systém bude nastaven na použití profilu `performance`, dokud hra běží.
Dříve použitý napájecí profil bude obnoven po zavření hry.

*[GameMode od Feral](https://github.com/FeralInteractive/gamemode)
se chová podobně, ale neměl by být používán, protože CachyOS již dodává [`ananicy-cpp`](https://gitlab.com/ananicy-cpp/ananicy-cpp)*

### Jak přidat game-performance do Steamu, Lutrisu a Heroic Games Launcheru

<Tabs>

<TabItem label='Steam'>

<Steps>
1. Otevřete svou `knihovnu Steamu`.
2. Klikněte pravým tlačítkem na název hry a vyberte `Vlastnosti`.
3. Na kartě `Obecné` najdete sekci `Možnosti spuštění`.
4. Přidejte následující možnost spuštění:
    ```sh
    game-performance %command%
    ```
</Steps>

</TabItem>

<TabItem label='Heroic Games Launcher'>

<Steps>
1. V levém panelu otevřete `Nastavení`.
2. Přejděte na `Výchozí nastavení her` a poté klikněte na `Pokročilé`.
3. V sekci `Příkaz wrapperu`. Přidejte následující řádek bez argumentů:
   ```sh
   game-performance
   ```
4. Klikněte na znaménko `+` pro uložení změn.
</Steps>

</TabItem>

<TabItem label='Lutris'>

<Steps>
1. Vpravo nahoře otevřete `hamburger menu`.
2. Přejděte na `Předvolby/Globální možnosti`.
3. Vpravo nahoře povolte `Pokročilý režim`.
4. Sjeďte dolů na `Prefix příkazu` a přidejte následující řádek:
   ```sh
   game-performance
   ```
5. Uložte změny.
</Steps>

</TabItem>

</Tabs>

## Proton-CachyOS

:::caution
Pokud se setkáte s problémy u her používajících **Easy Anti-Cheat** (EAC) nebo **BattlEye** (BE), například odmítání přihlášení na herní servery, můžete použít verzi [Proton-CachyOS sestavenou s použitím Steam Linux Runtime](https://github.com/CachyOS/proton-cachyos/releases)
:::

Proton-CachyOS je založen na `bleeding-edge` větvi Protonu a aplikuje na ni řadu úprav.
- **Patche z Wine-staging**
- **Wine Fullscreen FSR**
- **Zahrnuje video a audio kodeky pro herní cutscény**
- **Podpora pro `umu-launcher` včetně UMU-Protonfixes**
- **Přidává rané opravy/náhradní řešení pro hry**

:::note
Balíček `proton-cachyos` z repozitářů CachyOS je kompilován proti nativním systémovým knihovnám, nepoužívá Steam Linux Runtime. Cílová architektura závisí na repozitáři. V současné době repozitáře `x86_64_v3`, `x86_64_v4` a `znver4` používají stejné binární soubory `x86_64_v3`.
:::

**Další možnosti konfigurace**

- `PROTON_NO_WM_DECORATION=1`: Zakáže dekorace oken pomocí linuxového správce oken. Může opravit problémy s **bezrámečkovým režimem na celou obrazovku** a proklikáváním myši skrz okno.
- `PROTON_PREFER_SDL_INPUT=1`: Povolte pro obejití problémů se správnou detekcí ovladače.

### Nastavení umu-launcher

:::note
Ujistěte se, že máte v systému nainstalovaný umu-launcher z CachyOS. Nainstalujte jej následujícím příkazem:
`sudo pacman -S cachyos/umu-launcher`

Nezapomeňte zahrnout proměnnou prostředí `UMU_NO_RUNTIME`
:::

<Tabs>
    <TabItem label='Lutris Globální'>
    <Steps>
    1. Na hlavní obrazovce Lutrisu klikněte na `ozubené kolečko` vpravo od **Wine**.
    2. Proveďte následující změny v záložce **Možnosti spouštěče**:
        - **Verze Wine** = `proton-cachyos`
        - **Použít systémový winetricks** = *Vypnuto*
        - **Grafika**
            - **Povolit DXVK** = `Zapnuto`
            - **Povolit VKD3D** = `Zapnuto`
            - **Povolit D3D Extras** = `Zapnuto`
            - **Povolit DXVK-NVAPI / DLSS** = `Zapnuto`
            - Uživatelsky definované verze **DXVK**, **VKD3D** a **DXVK-NVAPI** se nepoužijí při použití `umu-launcher`.
    3. Přejděte do záložky **Systémové možnosti**.
        - **Lutris**
            - **Zakázat Lutris Runtime** = `Zapnuto`
            - **Preferovat systémové knihovny** = `Zapnuto`
    4. Sjeďte dolů na **Spouštění hry** a najděte tabulku **Proměnné prostředí**.
    5. Přidejte následující proměnné prostředí:
        - **Klíč**: `UMU_NO_RUNTIME`
            - **Hodnota**: `1` *použije systémové knihovny místo steam runtime, pouze pro proton-cachyos*
        - **Klíč**: `UMU_RUNTIME_UPDATE` *volitelné*
            - **Hodnota**: `0` *přeskočí aktualizace Steam Linux Runtime pro `proton-cachyos`*
        - **Klíč**: `PROTON_VERB` *volitelné*
            - **Hodnota**: `waitforexitandrun` *umožňuje protonfixes fungovat s odpovídajícím `GAMEID`*
    6. Uložte změny.
    </Steps>
    </TabItem>
    <TabItem label='Lutris pro jednotlivou hru'>
    <Steps>
    1. Klikněte pravým tlačítkem na hru, kterou chcete nakonfigurovat, a poté klikněte na **Konfigurovat**.
    2. Proveďte následující změny v záložce **Možnosti spouštěče**:
        - **Verze Wine** = `proton-cachyos`
        - **Použít systémový winetricks** = *Vypnuto*
        - **Grafika**
            - **Povolit DXVK** = `Zapnuto`
            - **Povolit VKD3D** = `Zapnuto`
            - **Povolit D3D Extras** = `Zapnuto`
            - **Povolit DXVK-NVAPI / DLSS** = `Zapnuto`
            - Uživatelsky definované verze **DXVK**, **VKD3D** a **DXVK-NVAPI** se nepoužijí při použití `umu-launcher`.
    3. Přejděte do záložky **Systémové možnosti**.
        - **Lutris**
            - **Zakázat Lutris Runtime** = `Zapnuto`
            - **Preferovat systémové knihovny** = `Zapnuto`
    4. Sjeďte dolů na **Spouštění hry** a najděte tabulku **Proměnné prostředí**.
    5. Přidejte následující proměnné prostředí:
        - **Klíč**: `UMU_NO_RUNTIME`
            - **Hodnota**: `1` *pokud používáte umu-launcher `1.1.4` z CachyOS*
        - **Klíč**: `UMU_RUNTIME_UPDATE` *volitelné*
            - **Hodnota**: `0` *přeskočí aktualizace Steam Linux Runtime pro `proton-cachyos`*
        - **Klíč**: `PROTON_VERB` *volitelné*
            - **Hodnota**: `waitforexitandrun` *umožňuje protonfixes fungovat s odpovídajícím `GAMEID`*
    6. Uložte změny.
    </Steps>
    </TabItem>
    <TabItem label='Heroic Games Launcher'>
        <Steps>
            1. Klikněte na tlačítko `Konfigurovat` vedle tlačítka `Hrát nyní` u hry, kterou chcete spustit.
            2. V záložce `WINE` nastavte verzi Wine na `Proton - proton-cachyos`.
            3. Přejděte na `Pokročilé`. Sjeďte dolů na `Proměnné prostředí`.
            4. Přidejte `UMU_NO_RUNTIME` do názvu proměnné s hodnotou `1` a klikněte na znaménko plus.
        </Steps>
    </TabItem>
</Tabs>

### Jak nainstalovat proton-cachyos-slr

<Tabs>

<TabItem label='Automaticky (protonup)'>

<Steps>
1. Nainstalujte **protonup-qt**
    ```sh
    sudo pacman -S protonup-qt
    ```
2. Otevřete **protonup-qt** a postupujte podle snímku obrazovky:
    <ImageComponent imgsrc={import('~/assets/images/steam-protonup.png')} />
    :::note
    Zvolte verzi `x86-64_v3`, pokud vaše CPU podporuje **[AVX2](/installation/installation_prepare#x86_64-microarchitecture-level-support)**.

    V opačném případě stáhněte tu, která končí na `x86-64`.
    :::
3. Restartujte Steam, pokud jste ho měli otevřený.

</Steps>

</TabItem>

<TabItem label='Ručně'>

<Steps>

1. Stáhněte si nejnovější verzi [zde.](https://github.com/CachyOS/proton-cachyos/releases) (**`Sjeďte dolů na Assets`**)

    :::note
    Vyberte tu, která končí na `x86-64_v3`, pokud vaše CPU podporuje **[AVX2](/installation/installation_prepare#x86_64-microarchitecture-level-support)**

    V opačném případě stáhněte tu, která končí na `x86-64`.
    :::
2. Dekomprimujte soubor a přesuňte složku do `~/.steam/steam/compatibilitytools.d/`
3. Restartujte Steam, pokud jste ho měli otevřený.

</Steps>

</TabItem>

</Tabs>

## Wine-CachyOS

Toto je stejné `wine`, které je jádrem `proton-cachyos`, ale jako samostatný balíček, který lze použít v Lutris, Heroic, Bottles a dalších.

- **Všechny úpravy Wine zahrnuté v Proton-CachyOS**
- **Přidává rané opravy/náhradní řešení pro hry**

:::note
V repozitářích CachyOS je **Wine-CachyOS** nabízeno dvěma různými balíčky. `wine-cachyos` ho instaluje jako výchozí systémové Wine a nahrazuje `wine` nebo `wine-staging`. `wine-cachyos-opt` ho instaluje do jiného umístění, což umožňuje koexistenci více verzí Wine v systému.
:::

:::caution
Primárním účelem **Wine-CachyOS** je **hraní her**. Ačkoli může být použit jako systémové Wine s desktopovými aplikacemi, mějte na paměti, že to může vést k neočekávanému chování a problémům kvůli úpravám zaměřeným na hry.

Pokud chcete používat desktopové aplikace, doporučujeme použít `wine` nebo `wine-staging` jako systémové Wine a nainstalovat `wine-cachyos-opt` pro hraní her.
:::

**Další možnosti konfigurace**

- `WINE_WMCLASS="<name>"`: Nastaví `WM_CLASS` všech oken Wine, což umožňuje správci oken ovládat okna Wine pomocí pravidel.
- `WINEUSERSANDBOX=1`: Zakáže vytváření symbolických odkazů z uživatelských složek Wine, jako jsou Dokumenty/Obrázky, do ekvivalentních složek v `HOME` adresáři uživatele.
- `WINE_NO_WM_DECORATION=1`: Zakáže dekorace oken pomocí linuxového správce oken. Může opravit problémy s **bezrámečkovým režimem na celou obrazovku** a proklikáváním myši skrz okno.
- `WINE_PREFER_SDL_INPUT=1`: Povolte pro obejití problémů se správnou detekcí ovladače.

### Jak používat wine-cachyos-opt

<Tabs>
    <TabItem label='Samostatně'>
    Obvykle by mělo stačit spustit `/opt/wine-cachyos/bin/wine` místo pouhého `wine`, aby aplikace běžela s použitím `wine-cachyos-opt`.

    Pokud je vyžadována přísnější konfigurace, mohla by vypadat takto:
    ```shell
    export PATH="/opt/wine-cachyos/bin/:$PATH"
    export WINEDLLPATH="/opt/wine-cachyos/lib/wine:/opt/wine-cachyos/lib32/wine:$WINEDLLPATH"
    export LD_LIBRARY_PATH="/opt/wine-cachyos/lib/:/opt/wine-cachyos/lib32/:$LD_LIBRARY_PATH"
    ```

    Pokud chcete použít `winetricks` s `wine-cachyos-opt`, můžete ho vyvolat takto:
    ```shell
    WINE=/opt/wine-cachyos/bin/wine WINEPRFIX=<váš prefix> winetricks <verb>
    ```
    </TabItem>
    <TabItem label='Lutris'>
      :::note
      Druhý obrázek platí i pro nastavení pro jednotlivou hru.
      :::
      <MultipleImageComponent
      images={[
      import('~/assets/images/lutris-guide-1.png'),
      import('~/assets/images/lutris-guide-2.png')]}
      />
    </TabItem>
    <TabItem label='Heroic Games Launcher'>
      <MultipleImageComponent
      images={[
      import('~/assets/images/heroic-wine.png'),
      import('~/assets/images/heroic-wine-2.png'),
      import('~/assets/images/heroic-wine-3.png')]}
      />
    </TabItem>
</Tabs>

## Úvod a tipy pro Lutris

Lutris slouží jako centrální místo pro všechny vaše hry na CachyOS.

S Lutrisem můžete efektivně spravovat své spouštěče her, včetně Wine, Protonu a emulátorů.

- **Můžete spouštět hry přes Lutris jednoduše kliknutím na tlačítko `Hrát`.**
- **Přidejte jakoukoli hru, kterou chcete, kliknutím na znaménko `+` vlevo nahoře.**
- **Nastavte si obchod v sekci `Zdroje` v levém panelu a připojte svůj účet, poté se nainstaluje daný obchod a budete moci spouštět hry z obchodu, stejně jako na Windows.**
- **A mnohem více!**

### Herní obchody podporované v Lutrisu

- [EA App](https://lutris.net/games/ea-app/)
- [Epic Games Store](https://lutris.net/games/epic-games-store/)
- [GOG Galaxy](https://lutris.net/games/gog-galaxy/)
- [Steam](https://lutris.net/games/steam/)
- [Ubisoft Connect](https://lutris.net/games/ubisoft-connect/)

### Přidání parametru do hry

- Klikněte pravým tlačítkem na ikonu hry > `Konfigurovat` > `Možnosti hry` > `Argumenty` > Přidejte jakýkoli argument, který potřebujete.

## Podpora Ray tracingu
Arch Wiki již poskytuje komplexní pokyny, jak povolit [ray tracing](https://wiki.archlinux.org/title/Hardware_raytracing) pro různé hardwarové platformy.
- [Ray tracing na NVIDIA](https://wiki.archlinux.org/title/Hardware_raytracing#NVIDIA)
- [Ray tracing na AMD](https://wiki.archlinux.org/title/Hardware_raytracing#AMD)
- [Ray tracing na Intel](https://wiki.archlinux.org/title/Hardware_raytracing#Intel)

**S tímto průvodcem jste nyní připraveni začít hrát na CachyOS. Užijte si to!**

## Vynucení nejnovějšího přednastavení DLSS

CachyOS poskytuje wrapper skript `dlss-swapper` pro to, aby hry podporující DLSS 2 a novější využívaly nejnovější přednastavení, což zlepšuje vizuální čistotu (ale potenciálně snižuje výkon, zejména na starších kartách).

Pokud `dlss-swapper` nefunguje nebo způsobuje problémy, zkuste ručně aktualizovat implementaci DLSS ve hře nahrazením `nvngx_dlss.dll` aktuální verzí a místo toho použijte wrapper skript `dlss-swapper-dll`.

### Jak přidat dlss-swapper do Steamu, Lutrisu a Heroic Games Launcheru

<Tabs>

<TabItem label='Steam'>

<Steps>
1. Otevřete svou `knihovnu Steamu`.
2. Klikněte pravým tlačítkem na název hry a vyberte `Vlastnosti`.
3. Na kartě `Obecné` najdete sekci `Možnosti spuštění`.
4. Přidejte následující možnost spuštění:
    ```sh
    dlss-swapper %command%
    ```
</Steps>

</TabItem>

<TabItem label='Heroic Games Launcher'>

<Steps>
1. V levém panelu otevřete `Nastavení`.
2. Přejděte na `Výchozí nastavení her` a poté klikněte na `Pokročilé`.
3. V sekci `Příkaz wrapperu`. Přidejte následující řádek bez argumentů:
   ```sh
   dlss-swapper
   ```
4. Klikněte na znaménko `+` pro uložení změn.
</Steps>

</TabItem>

<TabItem label='Lutris'>

<Steps>
1. Vpravo nahoře otevřete `hamburger menu`.
2. Přejděte na `Předvolby/Globální možnosti`.
3. Vpravo nahoře povolte `Pokročilý režim`.
4. Sjeďte dolů na `Prefix příkazu` a přidejte následující řádek:
   ```sh
   dlss-swapper
   ```
5. Uložte změny.
</Steps>

</TabItem>

</Tabs>
