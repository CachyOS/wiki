---
title: Установка CachyOS на настольный компьютер/ноутбук
description: Как установить CachyOS на настольный компьютер или ноутбук
sidebar:
  order: 1
tableOfContents:
  minHeadingLevel: 1
  maxHeadingLevel: 4
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

## Перед началом

:::note
Если после нажатия **Launch Installer** (Запустить установщик) в качестве вариантов загрузчика предлагаются только GRUB и Limine, значит, система загружена в режиме Legacy/BIOS.
:::

:::note[Установка нескольких сред рабочего стола в процессе инсталляции не допускается.]
:::

:::caution[При установке в режиме UEFI в настройках BIOS/UEFI необходимо отключить Secure Boot и CSM.]
Чтобы настроить Secure Boot после установки, обратитесь к разделу [Настройка Secure Boot](/ru/configuration/secure_boot_setup).

Параметр `Legacy USB Support` должен быть установлен в `Auto`.
:::

:::caution
Опции `Install alongside` (Установить рядом) и `Replace partition` (Заменить раздел) не являются на 100% надежными и могут привести к сбою установки.

При установке CachyOS **настоятельно рекомендуется** использовать `Manual partitioning` (Ручная разметка).
:::

:::tip
Настоятельно рекомендуется перезагрузиться с ISO-образа после неудачной попытки установки.

В противном случае в процессе установки могут возникать постоянные ошибки.

Это происходит потому, что установщик не может корректно размонтировать разделы при сбое установки.

:::

## Способы установки

### Ручная разметка

:::note[Прежде чем приступать к разметке диска для установки в режиме UEFI.]
    ```sh title='Выполните эту команду в терминале'
    efibootmgr -v
    ```
Если в выводе появляется `EFI variables are not supported on this system.` (Переменные EFI не поддерживаются в этой системе), значит, система загружена в режиме Legacy/BIOS, и вам необходимо убедиться, что в настройках BIOS/UEFI включен режим UEFI. Обратитесь к разделу [Требования](/ru/installation/installation_on_root/#перед-началом).
:::

<Tabs>

Таблица разделов для каждого загрузчика отличается. Следуйте инструкциям в **Шаге 5** для желаемого загрузчика.

<TabItem label='UEFI/GPT'>

<Steps>

1. Загрузитесь с ISO-образа и нажмите **Launch Installer** (Запустить установщик).

2. Установите предпочитаемый **Language** (Язык) и **Region/Timezone** (Регион/Часовой пояс).

3. Настройте **Keyboard Layout** (Раскладку клавиатуры).

4. Выберите **Manual partitioning** (Ручная разметка).

5. Создайте новый раздел со следующими параметрами в зависимости от выбранного загрузчика:
    <Tabs>
        <TabItem label='systemd-boot и rEFInd'>
            - ##### Размер: не менее **2048 МиБ**
            - ##### Файловая система: **FAT32**
            - ##### Точка монтирования: **/boot**
            - ##### Флаги: **boot**
        </TabItem>
        <TabItem label='Limine'>
            - ##### Размер: не менее **4096 МиБ**
            - ##### Файловая система: **FAT32**
            - ##### Точка монтирования: **/boot**
            - ##### Флаги: **boot**
        </TabItem>
        <TabItem label='GRUB'>
            - ##### Размер: не менее **512 МиБ**
            - ##### Файловая система: **FAT32**
            - ##### Точка монтирования: **/boot/efi**
            - ##### Флаги: **boot**
        </TabItem>
    </Tabs>

6. Создайте еще один раздел для **корня** (`root`):
   - ##### Размер: не менее **20000 МиБ**
   - ##### Файловая система: **Любая**, см. [Файловая система](/ru/installation/filesystem)
   - ##### Точка монтирования: **/**
   - ##### Флаги:

7. Выберите предпочитаемую [Среду рабочего стола](/ru/installation/desktop_environments).

8. Выберите, какие пакеты следует установить в процессе инсталляции.

9. Настройте учетные данные для входа.

10. Внимательно просмотрите сводку установки. Если все выглядит правильно, нажмите **Install Now** (Установить сейчас), чтобы продолжить установку. В противном случае вернитесь назад и внесите необходимые изменения.

</Steps>

</TabItem>

<TabItem label='MBR/BIOS'>
<Steps>

1. Загрузитесь с ISO-образа и нажмите **Launch Installer** (Запустить установщик).

2. Установите предпочитаемый **Language** (Язык) и **Region/Timezone** (Регион/Часовой пояс).

3. Настройте **Keyboard Layout** (Раскладку клавиатуры).

4. Выберите **Manual partitioning** (Ручная разметка).

5. Создайте новый раздел со следующими параметрами в зависимости от выбранного загрузчика:
    <Tabs>
        <TabItem label='GRUB'>
            - ##### Размер: не менее **20000 МиБ**
            - ##### Файловая система: **Любая**, см. [Файловая система](/ru/installation/filesystem)
            - ##### Точка монтирования: **/**
            - ##### Флаги:
        </TabItem>
        <TabItem label='Limine'>
            - ##### Размер: не менее **4096 МиБ**
            - ##### Файловая система: **FAT32**
            - ##### Точка монтирования: **/boot**
            - ##### Флаги: **boot**
            **Для корневой файловой системы требуется еще один раздел со следующими параметрами:**
            - ##### Размер: не менее **20000 МиБ**
            - ##### Файловая система: **Любая**, см. [Файловая система](/ru/installation/filesystem)
            - ##### Точка монтирования: **/**
            - ##### Флаги:
        </TabItem>
    </Tabs>
6. Дважды проверьте, что в поле **Install boot loader on:** (Установить загрузчик на:) указан ваш загрузочный диск, например, **/dev/sda**.

7. Выберите предпочитаемую [Среду рабочего стола](/ru/installation/desktop_environments).

8. Выберите, какие пакеты следует установить в процессе инсталляции.

9. Настройте учетные данные для входа.

10. Внимательно просмотрите сводку установки. Если все выглядит правильно, нажмите **Install Now** (Установить сейчас), чтобы продолжить установку. В противном случае вернитесь назад и внесите необходимые изменения.

</Steps>

</TabItem>

</Tabs>

## Двойная загрузка Windows и CachyOS

:::note[Это руководство предполагает, что Windows уже установлена в системе, и вы решили установить CachyOS рядом с ней на тот же диск.]
:::

:::caution
Двойная загрузка Windows и Linux не является на 100% надежной. Обновления Windows могут повредить загрузчик, перезаписав EFI-раздел.
В некоторых случаях проблемы могут никогда не возникнуть, но для некоторых пользователей это может стать постоянной проблемой.

Один из способов избежать этой проблемы — установить каждую операционную систему на отдельный диск.
:::

:::caution
Возможно, потребуется изменить размер EFI-раздела в зависимости от рекомендуемого минимального размера для целевого загрузчика. См. [Ручная разметка](/ru/installation/installation_on_root#ручная-разметка).

Файловые системы FAT нельзя изменять «на лету». Чтобы правильно «изменить размер» EFI-раздела с FAT, необходимо сделать резервную копию существующей файловой системы EFI, использовать инструмент, такой как `gparted`, для изменения размера существующего раздела и переформатирования нового раздела, а затем скопировать предыдущую структуру файловой системы обратно в EFI-раздел.
:::

### Предварительные требования

- Быстрый запуск Windows и гибернация должны быть отключены.
  - Откройте Windows Powershell от имени администратора и выполните следующую команду:
      ```ps
      powercfg /H off
      ```
    Перезагрузите систему, чтобы изменения вступили в силу.
- Windows BitLocker должен быть отключен.
  - Проверка, включен ли BitLocker:
    ```ps title='Откройте командную строку от имени администратора и выполните следующую команду'
    manage-bde -status
    ```
  Если в поле **Encryption Method** (Метод шифрования) указано `None`, значит, BitLocker отключен.
- Secure Boot должен быть отключен.
- Пустой раздел размером ≥30 ГБ.
  - Руководство по сжатию раздела Windows:
    - Нажмите `Win + R`, введите `diskmgmt.msc` и нажмите Enter, чтобы открыть «Управление дисками».
    - Найдите ваш основной раздел Windows (обычно C:) и щелкните по нему правой кнопкой мыши.
    - Нажмите `Shrink Volume...` (Сжать том...), укажите не менее **30720 МБ** (30 ГиБ) и нажмите `Shrink` (Сжать).
- Загрузочный USB-накопитель с CachyOS.
  - Обратитесь к разделу [Создание загрузочного USB-накопителя CachyOS](/ru/installation/installation_prepare#создание-загрузочного-usb-накопителя-cachyos).

<Tabs>
    <TabItem label='systemd-boot'>
        **Нам нужно скопировать бинарные файлы Windows EFI в раздел Linux EFI, чтобы загрузчик мог их распознать.**

        <Steps>
        1. Найдите **раздел Windows EFI** с помощью `lsblk`.

            ```sh title='Выполните эту команду в терминале'
            lsblk -o NAME,FSTYPE,SIZE,MOUNTPOINT
            ```
            ```sh title='Пример вывода'
                        NAME        FSTYPE   SIZE MOUNTPOINT
            zram0               15.3G [SWAP]
            nvme0n1            476.9G
            ├─nvme0n1p1 vfat     100M
            ├─nvme0n1p2           16M
            ├─nvme0n1p3 ntfs   234.4G
            ├─nvme0n1p4 ntfs     830M
            ├─nvme0n1p5 vfat       2G /boot
            └─nvme0n1p6 btrfs  239.6G /var/tmp
            ```
            EFI-разделы почти всегда отформатированы как `FAT32/vfat`. Поскольку у `nvme0n1p1` нет точки монтирования в Linux, мы можем предположить, что этот раздел является разделом Windows EFI.

        2. Временно смонтируйте раздел Windows EFI.

            ```sh
            sudo mkdir /mnt/WinBoot
            sudo mount /dev/nvme0n1p1 /mnt/WinBoot # Замените `nvme0n1p1` на имя раздела Windows EFI.
            ```

        3. Скопируйте бинарные файлы EFI из **раздела Windows EFI** в **раздел Linux EFI**.

            ```sh
            sudo cp -r /mnt/WinBoot/EFI/Microsoft/ /boot/EFI
            ```

        4. Размонтируйте ранее смонтированный раздел. Windows должна появиться в меню загрузки при следующем запуске.

            ```sh
            sudo umount /mnt/WinBoot
            sudo rm -r /mnt/WinBoot
            ```

        </Steps>
    </TabItem>

    <TabItem label='GRUB'>
        **GRUB использует os-prober для автоматического обнаружения раздела Windows EFI и добавления его в меню загрузки.**

        <Steps>
        1. Установите **os-prober**.

            ```sh
            sudo pacman -S os-prober
            ```

        2. Включите **os-prober** в файле конфигурации GRUB.

            ```sh title='Нажмите CTRL+S для сохранения и CTRL+Q для выхода из Micro'
            sudo micro /etc/default/grub
            # /etc/default/grub
            # Probing for other operating systems is disabled for security reasons. Read
            # documentation on GRUB_DISABLE_OS_PROBER, if still want to enable this
            # functionality install os-prober and uncomment to detect and include other
            # operating systems.
            GRUB_DISABLE_OS_PROBER=false

            ```

        3. Обновите grub.cfg, теперь с использованием **os-prober**.
            ```sh
            sudo grub-mkconfig -o /boot/grub/grub.cfg
            ```

            **Теперь Windows должна быть добавлена в меню загрузки.**

        </Steps>
    </TabItem>
</Tabs>
