---
title: KDE Plasma
description: Раздел помощи по KDE Plasma в CachyOS.
ai_translated: true
---

import ImageComponent from '~/components/image-component.astro';
import { Tabs, TabItem, Steps } from '@astrojs/starlight/components';

## Как установить недостающую сессию X11

После того как разработчики KDE решили разделить свою разработку на несколько пакетов, сессия X11 была удалена из стандартной установки.

```sh
sudo pacman -S plasma-x11-session kwin-x11
```
По завершении X11 будет указан как вариант на экране входа. 

## Как установить недостающий xorg-server

Начиная с версии 26.01 установщика CachyOS xorg-server больше не устанавливается, и ни plasma-x11-session, ни kwin-x11 не рассматривают xorg-server как зависимость.  В результате, если не установить вручную сервер Xorg и связанные с ним компоненты, при входе появится чёрный экран.  

```sh
sudo pacman -S xorg-server
```

## FAQ

### Как мне получить такой же вид, как на скриншотах KDE, под названием "Emerald"?

- Это комбинация темы Emerald и темы иконок Qogir, которые не установлены по умолчанию.
  - ```sh title='Установите обе с помощью следующей команды'
    sudo pacman -S cachyos-emerald-kde-theme-git qogir-icon-theme
    ```
  Примените обе в `Внешний вид и стиль - Цвета и темы`

### Экран входа KDE не отражает внешний вид моего рабочего стола, как это исправить?

:::tip[Это работает как для SDDM, так и для Plasma Login Manager.]
:::

Чтобы отразить рабочий стол на экране входа, следуйте скриншоту ниже:

<ImageComponent imgsrc={import('~/assets/images/kde-login-screen-settings.png')} />
