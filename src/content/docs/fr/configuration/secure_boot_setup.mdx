---
title: Configuration du Secure Boot
description: Configurer le Secure Boot avec sbctl après avoir installé CachyOS
---

import ImageComponent from '~/components/image-component.astro';

# sbctl

[`sbctl`](https://github.com/Foxboron/sbctl) est un gestionnaire de clés Secure Boot convivial capable de configurer le Secure Boot,
offrant des capacités de gestion de clés et gardant une trace des fichiers qui doivent être signés dans la chaîne de démarrage.

## Installation de sbctl

```bash
❯ sudo pacman -S sbctl
```

## Pré-configuration

### Gestionnaire de démarrage GRUB

Si vous utilisez GRUB, exécutez la commande suivante pour activer la prise en charge du Secure Boot sur GRUB en utilisant les clés CA.

```bash
❯ sudo grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=cachyos --modules="tpm" --disable-shim-lock
```

:::note
Le chargement de modules inutiles dans votre gestionnaire de démarrage peut présenter un risque pour la sécurité.
N'exécutez cette commande que si vous avez réellement besoin du Secure Boot.
:::

### Activer le mode Setup dans l'UEFI
Tout d'abord, nous devons aller dans les paramètres du firmware et régler le mode Secure Boot sur « Setup Mode » (mode de configuration). Vous pouvez redémarrer depuis un
système déjà en cours d'exécution vers les paramètres du firmware avec la commande suivante.

```bash
❯ systemctl reboot --firmware-setup
```

<br />
<ImageComponent imgsrc={import('~/assets/images/ideapad-bios-secure-boot.jpg')} />

Voici à quoi ressemble le BIOS sur un Lenovo Ideapad 5 Pro. Réinitialisez en mode de configuration ou restaurez les clés d'usine, puis redémarrez
le système.

## Configuration de sbctl

```bash
❯ sudo sbctl status # Si le mode de configuration est activé, nous pouvons passer à l'étape suivante
Installed: ✘ sbctl is not installed
Setup Mode: ✘ Enabled
Secure Boot ✘ Disabled

❯ sudo sbctl create-keys # Créez vos clés Secure Boot personnalisées
Created Owner UUID a9fbbdb7-a05f-48d5-b63a-08c5df45ee70
Creating secure boot keys...✔
Secure boot keys created!

❯ sudo sbctl enroll-keys --microsoft # Enregistrez vos clés avec celles de Microsoft
Enrolling keys to EFI variables...✔
Enrolled keys to the EFI variables!

❯ sudo sbctl status
# sbctl devrait maintenant être installé, nous pouvons passer à la signature des images du noyau et du gestionnaire de démarrage
Installed: ✔ sbctl is installed
Owner GUID: a9fbbdb7-a05f-48d5-b63a-08c5df45ee70
Setup Mode: ✔ Disabled
Secure Boot ✘ Disabled
Vendor Keys: microsoft
```

## Signature de l'image du noyau et du gestionnaire de démarrage

CachyOS fournit [`sbctl-batch-sign`](https://github.com/CachyOS/CachyOS-Settings/blob/master/usr/bin/sbctl-batch-sign),
un script qui prend la liste des fichiers à signer depuis `sudo sbctl verify` et les signe tous.

```bash
❯ sudo sbctl verify
Verifying file database and EFI images in /boot...
✘ /boot/1c4b5246eef05ac3bc87339323cd5101/6.10.0-cn4.0.fc40.x86_64/linux is not signed
✘ /boot/EFI/BOOT/BOOTX64.EFI is not signed
✘ /boot/EFI/systemd/systemd-bootx64.efi is not signed
✘ /boot/1c4b5246eef05ac3bc87339323cd5101/0-rescue/linux is not signed
✘ /boot/1c4b5246eef05ac3bc87339323cd5101/6.10.0-cn3.0.fc40.x86_64/linux is not signed

❯ sudo sbctl-batch-sign

❯ sudo sbctl verify
Verifying file database and EFI images in /boot...
✔ /boot/1c4b5246eef05ac3bc87339323cd5101/6.10.0-cn4.0.fc40.x86_64/linux is signed
✔ /boot/EFI/BOOT/BOOTX64.EFI is signed
✔ /boot/EFI/systemd/systemd-bootx64.efi is signed
✔ /boot/1c4b5246eef05ac3bc87339323cd5101/0-rescue/linux is signed
✔ /boot/1c4b5246eef05ac3bc87339323cd5101/6.10.0-cn3.0.fc40.x86_64/linux is signed
```

:::caution
Sur les systèmes avec des partitions `/boot` et `/boot/efi` séparées, `sbctl` pourrait ne scanner les binaires EFI que dans `/boot/efi`.
Cela empêche la détection des images de noyau qui se trouvent dans `/boot`. `sbctl-batch-sign` contourne ce problème en scannant **toujours**
`/boot` à la recherche de fichiers `vmlinuz-*`.
:::

Maintenant que tous les fichiers sont signés, nous pouvons redémarrer dans les paramètres UEFI et activer le Secure Boot.
Notez qu'il s'agit d'un processus unique, car la signature des fichiers avec l'option `-s` enregistrera ces fichiers dans la base de données de `sbctl`.
`sbctl` est livré avec un [hook Pacman](https://wiki.archlinux.org/title/Pacman_hook) ce qui signifie qu'il signera automatiquement
tous les nouveaux fichiers lors d'une mise à jour du noyau ou du gestionnaire de démarrage.

### systemd-boot

CachyOS utilise `systemd-boot-update.service` fourni par systemd pour mettre à jour le gestionnaire de démarrage au redémarrage. Cela signifie que le hook
Pacman de `sbctl` **ne signera pas** les binaires EFI mis à jour. Pour contourner ce problème, nous pouvons signer directement le gestionnaire de démarrage

```sh
❯ sudo sbctl sign -s -o /usr/lib/systemd/boot/efi/systemd-bootx64.efi.signed /usr/lib/systemd/boot/efi/systemd-bootx64.efi
```

### Limine

Limine est un gestionnaire de démarrage spécial qui permet de vérifier le hachage des images du noyau
et d'autres fichiers que Limine utilise au démarrage. Si cette option est activée, toute
sorte de configuration manuelle effectuée par l'utilisateur, par exemple la signature de l'image via
`sbctl-batch-sign`, modifiera le hachage des fichiers correspondants et
entraînera l'échec de la vérification de la somme de contrôle de Limine.

Cependant, la signature de ces fichiers n'est pas nécessaire sur Limine car il dispose d'un processus
de démarrage spécial qui contourne le chaînage EFI et les vérifications de signature. Les seuls binaires EFI
qui doivent être signés sont Limine lui-même et le binaire EFI de secours
présent sur tous les systèmes UEFI.

```sh
# Signer le binaire EFI spécial
❯ sudo sbctl sign -s /boot/EFI/BOOT/BOOTX64.EFI

# Utiliser limine-enroll-config pour signer le binaire EFI de Limine
# Ceci utilise sbctl en arrière-plan
❯ sudo limine-enroll-config
```

## Vérifier que le Secure Boot est activé

Pour vérifier que le Secure Boot est bien activé, vous pouvez exécuter l'une des commandes suivantes

```bash
❯ sudo sbctl status
Installed:      ✓ sbctl is installed
Owner GUID:     a9fbbdb7-a05f-48d5-b63a-08c5df45ee70
Setup Mode:     ✓ Disabled
Secure Boot:    ✓ Enabled
Vendor Keys:    microsoft

❯ bootctl
System:
      Firmware: UEFI 2.80 (INSYDE Corp. 28724.16435)
 Firmware Arch: x64
   Secure Boot: enabled (user)
  TPM2 Support: yes
  Measured UKI: no
  Boot into FW: supported
```

## Liens et crédits

- [Le Wiki Arch](https://wiki.archlinux.org/title/Unified_Extensible_Firmware_Interface/Secure_Boot#Assisted_process_with_sbctl)
a servi de base à ce guide. La plupart des informations ici en proviennent.
- [sbctl](https://github.com/Foxboron/sbctl) - Ce guide facile pour activer la prise en charge du Secure Boot n'aurait pas été possible sans
le travail incroyable accompli pour créer ce logiciel.
- [Improving the Secure Boot Experience par Morten linderud](https://linderud.dev/blog/improving-the-secure-boot-user-experience/) - Article de blog de Morten
"Foxboron" Linderud sur la complexité de l'expérience Secure Boot avant `sbctl`.
