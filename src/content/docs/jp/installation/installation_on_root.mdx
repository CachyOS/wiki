---
title: Wie man CachyOS installiert & Partitionierungsanleitung
description: Festplatte löschen Installation
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

:::caution
Secure Boot muss bei der Installation von CachyOS deaktiviert sein. Um Secure Boot nach der Installation einzurichten, siehe [Secure Boot Einrichtung](/de/configuration/secure_boot_setup).
:::

:::tip
Es wird dringend empfohlen, die ISO nach einem fehlgeschlagenen Installationsversuch neu zu starten.

Andernfalls können während des Installationsvorgangs fortlaufend Fehler auftreten.

Dies liegt daran, dass das Installationsprogramm Partitionen bei einer fehlgeschlagenen Installation nicht ordnungsgemäß aushängt.

:::

## Festplatte löschen

Die Option "Festplatte löschen" in Calamares löscht die ausgewählte Festplatte und installiert CachyOS auf dem Ziel.

<Steps>

1. Boote in die ISO und klicke auf **Installationsprogramm starten**.

2. Wähle den bevorzugten **Bootmanager**. Weitere Informationen findest du im Abschnitt [Bootmanager](/de/installation/boot_managers).

3. Stelle die bevorzugte **Sprache** und **Region/Zeitzone** ein.

4. Konfiguriere das **Tastaturlayout**.

5. Wähle **Festplatte löschen** und wähle ein [Dateisystem](/de/installation/filesystem).

6. Wähle die gewünschte **Desktop-Umgebung**, siehe [Desktop-Umgebungen](/de/installation/desktop_environments).

7. Passe an, welche Pakete während des Installationsvorgangs installiert werden sollen oder nicht.

8. Richte Anmeldeinformationen ein.

9. Überprüfe die Installationszusammenfassung auf der Übersichtsseite sorgfältig. Fahre mit der Installation fort, indem du auf ***Jetzt installieren*** klickst, wenn alles korrekt aussieht. Andernfalls gehe zurück und nimm alle notwendigen Änderungen vor.

</Steps>

## Manuelle Partitionierung

### UEFI/GPT

Die Partitionstabelle für jeden Bootmanager variiert. Bitte befolge die korrekten Anweisungen für jeden.

<Tabs>

<TabItem label='systemd-boot & rEFInd & Limine'>

<Steps>

1. Boote in die ISO und klicke auf die Schaltfläche **Installationsprogramm starten**.

2. Stelle die bevorzugte **Sprache** und **Region/Zeitzone** ein.

3. Konfiguriere das **Tastaturlayout**.

4. Wähle **Manuelle Partitionierung**.

5. Erstelle eine neue Partition mit Folgendem:
    * Größe: ***2048MiB***
    * Dateisystem: ***FAT32***
    * Mountpunkt: ***/boot***
    * Flags: **boot**

6. Erstelle eine weitere Partition für ***root***:
    * Größe: ***Mindestens 20000MiB***
    * Dateisystem: Beliebig, siehe [Dateisystem](/de/installation/filesystem)
    * Mountpunkt: ***/***
    * Flags:

7. Überprüfe, ob ***Bootloader installieren auf:*** auf /boot zeigt.

8. Wähle die gewünschte **Desktop-Umgebung**, siehe [Desktop-Umgebungen](/de/installation/desktop_environments).

9. Passe an, welche Pakete während des Installationsvorgangs installiert werden sollen oder nicht.

10. Richte Anmeldeinformationen ein.

11. Überprüfe die Installationszusammenfassung auf der Übersichtsseite sorgfältig. Fahre mit der Installation fort, indem du auf ***Jetzt installieren*** klickst, wenn alles korrekt aussieht. Andernfalls gehe zurück und nimm alle notwendigen Änderungen vor.

</Steps>

</TabItem>

<TabItem label='GRUB'>

<Steps>

1. Boote in die ISO und klicke auf die Schaltfläche **Installationsprogramm starten**.

2. Stelle die bevorzugte **Sprache** und **Region/Zeitzone** ein.

3. Konfiguriere das **Tastaturlayout**.

4. Wähle **Manuelle Partitionierung**.

5. Erstelle eine neue Partition mit Folgendem:
    * Größe: ***Mindestens 100MiB***
    * Dateisystem: ***FAT32***
    * Mountpunkt: ***/boot/efi***
    * Flags: **boot**

6. Erstelle eine weitere Partition für ***root***:
    * Größe: ***Mindestens 20000MiB***
    * Dateisystem: Beliebig, siehe [Dateisystem](/de/installation/filesystem)
    * Mountpunkt: ***/***
    * Flags:

7. Überprüfe, ob ***Bootloader installieren auf:*** auf /boot/efi zeigt.

8. Wähle die gewünschte **Desktop-Umgebung**, siehe [Desktop-Umgebungen](/de/installation/desktop_environments).

9. Passe an, welche Pakete während des Installationsvorgangs installiert werden sollen oder nicht.

10. Richte Anmeldeinformationen ein.

11. Überprüfe die Installationszusammenfassung auf der Übersichtsseite sorgfältig. Fahre mit der Installation fort, indem du auf ***Jetzt installieren*** klickst, wenn alles korrekt aussieht. Andernfalls gehe zurück und nimm alle notwendigen Änderungen vor.

</Steps>

</TabItem>

</Tabs>

### MBR/BIOS

<Tabs>

<TabItem label='GRUB'>

<Steps>

1. Boote in die ISO und klicke auf die Schaltfläche **Installationsprogramm starten**.

2. Stelle deine bevorzugte **Sprache** und **Region/Zeitzone** ein.

3. Konfiguriere dein **Tastaturlayout**.

4. Wähle **Manuelle Partitionierung**.

5. Erstelle eine neue Partition mit Folgendem:
    * Größe: ***Mindestens 20000MiB***
    * Dateisystem: Beliebig, siehe [Dateisystem](/de/installation/filesystem)
    * Mountpunkt: ***/***
    * Flags:

6. Überprüfe, ob ***Bootloader installieren auf:*** auf dein Bootlaufwerk zeigt, z. B.: /dev/sda

7. Wähle die **Desktop-Umgebung** aus, die du verwenden möchtest, siehe [Desktop-Umgebungen](/de/installation/desktop_environments).

8. Wähle die spezifischen Pakete aus, die du aus der bereitgestellten Liste installieren möchtest, und deaktiviere alle, die du nicht benötigst.

9. Richte deine Anmeldeinformationen ein.

10. Überprüfe die Installationszusammenfassung auf der Übersichtsseite sorgfältig. Wenn alle Einstellungen für dich korrekt aussehen, fahre mit der Installation fort, indem du auf ***Jetzt installieren*** klickst. Andernfalls gehe zurück und nimm alle notwendigen Änderungen vor.

</Steps>

</TabItem>

<TabItem label='Limine'>

<Steps>

1. Boote in die ISO und klicke auf die Schaltfläche **Installationsprogramm starten**.

2. Stelle deine bevorzugte **Sprache** und **Region/Zeitzone** ein.

3. Konfiguriere dein **Tastaturlayout**.

4. Wähle **Manuelle Partitionierung**.

5. Erstelle eine neue Partition mit Folgendem:
    * Größe: ***Mindestens 1000MiB***
    * Dateisystem: ***FAT32***
    * Mountpunkt: ***/boot***
    * Flags: **boot**

6. Erstelle eine weitere Partition für ***root***:
    * Größe: ***Mindestens 20000MiB***
    * Dateisystem: Beliebig, siehe [Dateisystem](/de/installation/filesystem)
    * Mountpunkt: ***/***
    * Flags:

7. Überprüfe, ob ***Bootloader installieren auf:*** auf dein Bootlaufwerk zeigt, z. B.: /dev/sda

8. Wähle die **Desktop-Umgebung** aus, die du verwenden möchtest, siehe [Desktop-Umgebungen](/de/installation/desktop_environments).

9. Wähle die spezifischen Pakete aus, die du aus der bereitgestellten Liste installieren möchtest, und deaktiviere alle, die du nicht benötigst.

10. Richte deine Anmeldeinformationen ein.

11. Überprüfe die Installationszusammenfassung auf der Übersichtsseite sorgfältig. Wenn alle Einstellungen für dich korrekt aussehen, fahre mit der Installation fort, indem du auf ***Jetzt installieren*** klickst. Andernfalls gehe zurück und nimm alle notwendigen Änderungen vor.

</Steps>

</TabItem>

</Tabs>

## Dual-Boot

*Nach der manuellen Partitionierung kann eine zusätzliche Einrichtung erforderlich sein, um ein Dual-Boot-System zu konfigurieren, insbesondere mit `systemd-boot` und `GRUB`.*

<Tabs>
    <TabItem label='systemd-boot'>
        *Wir müssen die Windows-EFI-Binärdateien in die Linux-EFI-Partition kopieren, damit der Bootmanager sie erkennen kann.*

        <Steps>
        1. Finde die `Windows-EFI-Partition` mit `lsblk`

            ```sh title='Beispiel'
            ❯ lsblk -o NAME,FSTYPE,SIZE,MOUNTPOINT
            NAME        FSTYPE   SIZE MOUNTPOINT
            zram0               15.3G [SWAP]
            nvme0n1            476.9G
            ├─nvme0n1p1 vfat     100M
            ├─nvme0n1p2           16M
            ├─nvme0n1p3 ntfs   234.4G
            ├─nvme0n1p4 ntfs     830M
            ├─nvme0n1p5 vfat       2G /boot
            └─nvme0n1p6 btrfs  239.6G /var/tmp
            ```

            *EFI-Partitionen sind fast immer als `FAT32/vfat` formatiert, und da nvme0n1p1 keinen Linux-Mountpunkt hat, gehe davon aus, dass diese Partition die `Windows-EFI-Partition` ist.*

        2. Hänge die `Windows-EFI-Partition` vorübergehend ein.

            ```sh
            ❯ sudo mount /dev/nvme0n1p1 /mnt/WinBoot # Ersetze `nvme0n1p1` durch die Partition der Windows-EFI-Partition
            ```

        3. Kopiere die EFI-Binärdateien von der `Windows-EFI-Partition` in die `Linux-EFI-Partition`.

            ```sh
            ❯ sudo cp -r /mnt/WinBoot/EFI/* /boot/EFI
            ```

        4. Hänge die zuvor eingehängte Partition aus, und Windows sollte beim nächsten Start im Bootmenü erscheinen.

            ```sh
            ❯ sudo umount /mnt/WinBoot
            ```

        </Steps>
    </TabItem>

    <TabItem label='GRUB'>
        *GRUB verwendet os-prober, um die Windows-EFI-Partition automatisch zu erkennen und sie dem Bootmenü hinzuzufügen.*

        <Steps>
        1. Installiere und führe `os-prober` aus

            ```sh
            ❯ sudo pacman -S os-prober
            ❯ sudo os-prober
            ```

        2. Aktiviere `os-prober` in der GRUB-Konfigurationsdatei

            ```sh title='Drücke STRG+S zum Speichern und STRG+Q zum Beenden von Micro'
            ❯ sudo micro /etc/default/grub
            # /etc/default/grub
            # Das Suchen nach anderen Betriebssystemen ist aus Sicherheitsgründen deaktiviert. Lies
            # die Dokumentation zu GRUB_DISABLE_OS_PROBER, wenn du diese Funktion dennoch aktivieren möchtest
            # installiere os-prober und entferne die Auskommentierung, um andere zu erkennen und einzubeziehen
            # Betriebssysteme.
            GRUB_DISABLE_OS_PROBER=false

            ❯ sudo grub-mkconfig -o /boot/grub/grub.cfg
            ```

            **Windows sollte jetzt dem Bootmenü hinzugefügt werden.**

        </Steps>
    </TabItem>
</Tabs>

## Mehr Informationen

- [Lord Vexys Dual-Boot-Installationsanleitung mit dem rEFInd-Bootmanager](https://www.youtube.com/watch?v=0Toaq3Vn5qs)
